services:
   tunnel:
    container_name: cloudflared-tunnel
    image: cloudflare/cloudflared
    restart: unless-stopped
    command: tunnel run
    environment:
      - TUNNEL_TOKEN
    networks:
      - proxy-network #connecting to same Nginx proxy manager network

networks:
  proxy-network:
    external: true
    name: proxy-network

